# The username who should own the process.
#
ISPN_SERVER_USER={{ ispn_user }}

# The amount of time to wait for startup
#
STARTUP_WAIT=30

# The amount of time to wait for shutdown
#
SHUTDOWN_WAIT=30

# Location to keep the console log
#
ISPN_SERVER_CONSOLE_LOG=/var/log/infinispan-server/console.log

ISPN_SERVER_HOME={{ ispn_home }}

ISPN_SERVER_SCRIPT=clustered.sh
ISPN_SERVER_CONFIG=clustered.xml

ISPN_SERVER_RUN_CONF="/etc/infinispan-server/clustered.conf"
